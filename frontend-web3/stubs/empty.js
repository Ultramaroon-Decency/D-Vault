// Stub module — @x402/* packages are optional peer deps of @coinbase/cdp-sdk,
// which is a transitive dependency pulled in by @base-org/account → @wagmi/connectors.
// We stub them out so the Next.js build succeeds without installing them.
module.exports = {};
